#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @n: first number input
 * @z: second number input
 * Return: n + z
 */
int add(int n, int z)
{
	return (n + z);
}
